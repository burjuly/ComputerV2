TODO

Умножение матриц:
A = [[2,3]; [3,4]]
B = [[1,0]; [0,1]]
A ** B = ? (ответ - матрица A)

f (x) = x + 2
p = 4
f (p) =? (результат 6)

f(x) = 2 * (x + 3 * (x - 4))
p = 2
f(3) - f(p) + 2 = ?  (результат 10)

2) Встроить решение линейных и квадратеых уравнений
3) Построение графиков



DONE:
1. Валидация
    1) Подготовка - убрать все пробелы
    2) Кол-во знаков =
    3) В строке только буквы, числа, операции и скобки
    4) Валидация скобок (открывающиеся = закрывающиеся ()[] )
    5) Проверка что по обе части от равно что-то есть

    6) Валидация если матрица пустая

2. Парсинг
    1) Переменные -> Сохранить их в словарь -> Вывод словаря по запросу -> переназначение переменных

3. Функции
    1) В правой части проверяем, что аргумент тот же что и в левой 
    (предварительно заменяем переменные значения, которых есть в словаре)
    Если f(x) -> В правой части допустимы только числа, операции и x

